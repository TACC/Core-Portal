repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.5
    hooks:
      # - id: ruff-check
      #   args: [--fix]
      #   files: ^server/
      - id: ruff-format
        files: ^server/
