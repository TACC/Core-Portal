# Optional Django DB pooling/env overrides.
# Point Django to PgBouncer instead of Postgres directly.

DJANGO_DB_HOST=pgbouncer
DJANGO_DB_PORT=6432

# Keep 0 for PgBouncer transaction pooling.
DJANGO_CONN_MAX_AGE=0
